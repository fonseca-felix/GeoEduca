import React from 'react'
import ReactDOM from 'react-dom/client'
import Index from './index'

ReactDOM.createRoot(document.getElementById('react-volcano-root')!).render(
  <React.StrictMode>
    <Index />
  </React.StrictMode>,
)
